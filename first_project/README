Instruções:

Execute main.py para executar o programa ( ./main.py ).

Você pode passar como argumento para main (argv) o arquivo de treinamento e o arquivo de dados a ser testado.
Caso nenhum arquivo seja especificado pela linha de comando, será assumido um arquivo de treinamento na pasta local com o nome de train_file, e um arquivo de teste chamado database.

É assumido que todo arquivo está no formado abaixo
0,0,0,aa
Ou seja, uma sequencia (n-dimensional) de números e um string.
Mesmo que o arquivo teste (database) não possua uma classe (o que faz sentido, já que o que se quer é justamente classificar aqueles dados), é assumido que existirá um string no final, portanto caso não exista uma classe prévia, faça um stript que dê um append ",a" ao final de cada linha para poder ser usado como entrada no programa.

Assim que o programa inicia você tem a opção de decidir qual algoritmo usar.

Divirta-se!
